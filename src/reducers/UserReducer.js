export default initialState = {
  avatar: "",
  favorites: [],
  appointments: [],
};

export const UserReducer = (state, action) => {
  switch (action.type) {
    case "setAvatar":
      return;
  }
};
